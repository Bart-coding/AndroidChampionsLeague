<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidchampionsleague">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidChampionsLeague">
        <activity android:name=".KnockoutMatchResultActivity"/>
        <activity android:name=".GroupMatchResultActivity" />
        <activity android:name=".AboutUsActivity" />
        <activity android:name=".GroupsActivity" />
        <activity android:name=".ResultsActivity" />
        <activity android:name=".NavigationActivity" />
        <activity android:name=".ScorersActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>